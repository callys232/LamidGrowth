import { OrbitVisual } from '../../../shared/visuals/Orbit';

/** Shared visual section for recovery, reset and verification pages. */
export function AccountVisualSlide() {
  return (
    <aside className="auth-aside">
      <OrbitVisual small />
    </aside>
  );
}
